# ❌ Vercel 部署问题解决方案

## 问题原因

**Vercel 不支持 Socket.IO 的 WebSocket 长连接！**

你的应用使用 Socket.IO 进行实时通信，需要持续运行的服务器来维持 WebSocket 连接。但是：

- ❌ Vercel 是 serverless 平台，函数是无状态的
- ❌ Serverless 函数不能维持长连接
- ❌ 每次请求都是独立的函数调用
- ❌ Socket.IO 需要持续运行的服务器进程

## ✅ 最简单的解决方案：使用 Render

Render 是免费的，支持 WebSocket，部署非常简单：

### 快速部署步骤（5 分钟）

1. **确保代码在 GitHub**
   ```bash
   git add .
   git commit -m "准备部署"
   git push
   ```

2. **访问 Render**
   - 打开 https://render.com
   - 注册/登录（可用 GitHub 账号）

3. **创建 Web Service**
   - 点击 "New +" → "Web Service"
   - 连接你的 GitHub 仓库
   - 选择你的 mahjong 仓库

4. **配置设置**
   - **Name**: `mahjong-game`（或任意名称）
   - **Region**: 选择离你最近的区域
   - **Branch**: `main`（或你的主分支）
   - **Root Directory**: 留空
   - **Environment**: `Node`
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Instance Type**: 选择 **Free**

5. **点击 "Create Web Service"**
   - 等待 2-5 分钟构建
   - 完成后会得到类似这样的 URL：`https://mahjong-game.onrender.com`

6. **完成！** ✅
   - 访问你的 URL 即可使用
   - 所有功能（包括联机）都会正常工作

### Render 免费版注意事项

- ✅ 完全免费使用
- ⚠️ 15 分钟无活动后会休眠
- ⚠️ 休眠后第一次访问需要几秒唤醒
- ✅ 如果经常使用，可以升级到付费版（约 $7/月）避免休眠

---

## 其他可选平台

### Railway
- 访问：https://railway.app
- 优点：免费额度，部署简单
- 步骤：New Project → Deploy from GitHub → 选择仓库 → 完成

### Fly.io
- 访问：https://fly.io
- 优点：全球部署，性能好
- 需要：安装 CLI，稍微复杂一些

---

## 如果一定要用 Vercel（不推荐）

你需要**拆分架构**：

1. **前端部署到 Vercel**
   - 只部署 `public/` 文件夹
   - 作为静态网站

2. **后端部署到 Render**
   - 部署整个项目（包括 server.js）

3. **修改 `public/client.js`**
   ```javascript
   // 将这行：
   const socket = io();
   
   // 改为：
   const socket = io('https://your-render-backend.onrender.com');
   ```

4. **修改 `server.js` 添加 CORS**
   ```javascript
   const io = socketIo(server, {
     cors: {
       origin: "https://your-vercel-app.vercel.app",
       methods: ["GET", "POST"]
     }
   });
   ```

**但这种方法更复杂，不如直接全部部署到 Render！**

---

## 📝 总结

**最简单的方案：直接在 Render 部署整个项目**

1. 推送到 GitHub
2. 在 Render 创建 Web Service
3. 连接 GitHub 仓库
4. 点击部署
5. 完成！

**你的应用就能正常联机了！** 🎉

